trigger:
  branches:
    include:
      - master

stages:
  - stage: ci
    displayName: CI
    jobs:
      - template: CI/SemanticRelease.yml
  
  - stage: cd
    displayName: CD
    jobs:
      - template: CD/SemanticRelease.yml
  
    # Publish to nuget